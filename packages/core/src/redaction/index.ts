/** The redaction layer's public surface. Deterministic by design, and the one exception to
 * the no-heuristics rule. */

export { buildFingerprint } from './fingerprint.js';

export { scanRedactionResidue } from './residue.js';
